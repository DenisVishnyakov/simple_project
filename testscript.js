'use strict';

const num = 4.2;
console.log(Math.round(num)); // 4 (округление)

const test = "12.2px";
console.log(parseInt(test)); // 12 (str to int)

const test = "12.2px";
console.log(parseFloat(test)); // 12 (str to int)
